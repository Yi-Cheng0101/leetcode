class Solution {
public:
    bool isAnagram(string s, string t) {
        sort(s.begin(), s.end(), less<char>());
        sort(t.begin(), t.end(), less<char>());
        if(s.compare(t)==0)
          return true;
        return false;
    }
};
